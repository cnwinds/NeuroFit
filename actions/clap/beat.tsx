/**
 * Clap 动作的节拍配置
 * BPM: 100
 * 自动生成于: 2025/12/26 13:53:15
 */

import { BeatPattern } from '../base/ActionBase';

export const ClapBeat: BeatPattern = {
  bpm: 100,
  timeSignature: [4, 4],
  pattern: [
    [{ type: 'kick', volume: 0.8 }],
    [{ type: 'snare', volume: 0.7 }],
    [],
    []
  ],
};
